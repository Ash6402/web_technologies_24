export const emailErrors = {
    invalid: "Invalid Email"
}

export const usernameErrors = {
    min: "Username must be atleast 3 characters long",
}

export const passwordErrors = {
    min: "Password must be 8 characters long"
}

export const confirmPassErrors = {
    missMatch: "Password does not match"
}